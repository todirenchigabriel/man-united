import React, { Component } from 'react';
import Slideshow from '../Slideshow';

class Home extends Component {
  render() {
    return (
      <div>
        <Slideshow />
      </div>
    );
  }
}

export default Home;
